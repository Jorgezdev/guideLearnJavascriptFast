Visit the official websites of Node

https://nodejs.org/es/download

-Download the avaible version that is compatible with your operating system (Linux, Mac or Windows)

-Check if the installation is was sucessful using the command line (cmd)

Enter the following the commands; it should return the version names of both node and npm.


Command: 
        node -v
        npm -v


It should versions of each one. After that, enter the command line:
node and type console.log to see if it displays a "hello world!" message.

